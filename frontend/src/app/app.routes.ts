import { Routes } from '@angular/router';

/**
 * Top-level route definitions for the Drive Lite application.
 *
 * Currently empty — routes will be added in Phase 2 when feature
 * modules (auth, dashboard, file-browser) are implemented.
 *
 * All feature routes will use lazy loading via `loadComponent` to keep
 * the initial bundle small.
 */
export const routes: Routes = [];
