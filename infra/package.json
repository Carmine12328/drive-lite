{
  "name": "@drive-lite/infra",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "AWS CDK Infrastructure for Drive Lite",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "vitest run",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "deploy:local": "cdklocal deploy",
    "lint": "eslint bin/ lib/ --no-error-on-unmatched-pattern"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.3.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^20.14.0",
    "aws-cdk": "^2.150.0",
    "eslint": "^10.8.1",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.66.0",
    "vitest": "^2.0.0"
  }
}
